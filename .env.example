# Environment variables are introduced only when an adapter requires them.
# Never store real credentials in this file.
ASYMMETRIC_ENV=development

# Required only when explicitly invoking the SEC evidence CLI.
# Replace this placeholder locally; it is a declared contact, not an API credential.
AIE_SEC_USER_AGENT=AIE-Research contact@example.com
